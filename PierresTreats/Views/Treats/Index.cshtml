@{
  Layout = "_Layout";
}

@using PierresTreats.Models;
@model List<PierresTreats.Models.Treat>;


<h1>Available Pastry List: </h1>

@if (@Model.Count == 0)
{
  <h3>There are no pastries available! Check back later.</h3>
}

<p>@Html.ActionLink("Add a new treat", "Create")</p>